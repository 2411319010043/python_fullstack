一、基础概念
    1.什么是数据分析
        把大量的数据进行统计和整理，得出结论为后续的决策提供数据支持

    2.anaconda安装

二、matplotlib
    1.绘制折线图 plt.plot
        from matplotlib import pyplot as plt

        设置图形大小 plt.figure()

        绘图 plt.plot(x,y)

        调整x(y)轴的刻度 plt.xticks()
            调整间距，添加字符串，添加中文的字体

        展示 plt.show()

        保存 plt.savefig(路径)

        显示中文 
            from matplotlib import font_manager
            my_font = font_manager.FontProperties(fname='C:/Windows/Fonts/simhei.ttf')

        一个图中绘制多个图形 plt.plot()调用多次
            图例 展示当前这个图形到底属于谁
                plot(label='名称')
                plot.legend(loc,propr) loc表示当前图例的位置

        图形样式 color
            linestyle
            linewidth
            alpha

        添加图形描述 plt.xlabel() plt.ylabel() plt.title()
        
        网格 plt.grid(alpha,linestyle) 调整网格透明度,网格线样式
