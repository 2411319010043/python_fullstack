1. _x :单前置下划线
        表示私有化属性或方法
        通过from somemodule import*禁止导入 别的模块导入后 这个变量导入不进去
        类对象和子类可以访问
2. __x : 双前置下划线
        避免与子类中的属性命名冲突
        无法在外部直接访问（通过名字重整实现）不会被别的子类继承
3. __x__ : 双前后下划线
        会被子类继承 属于魔法方法命名规则
4. x_ :单后置下划线
        关键字命名冲突情况下可以加_ 用来区分
5. x 公有变量